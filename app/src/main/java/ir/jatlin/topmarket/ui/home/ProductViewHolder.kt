package ir.jatlin.topmarket.ui.home

import ir.jatlin.topmarket.databinding.ProductCategoryItemViewBinding

class ProductViewHolder(
    private val binding: ProductCategoryItemViewBinding
) : BaseViewHolder<HomeItem.ProductItem>(binding) {

    init {
        // TODO: Set listeners and other configuration if needed
    }

    override fun bind(item: HomeItem.ProductItem) {
        // TODO: bind view with item
    }

}